---
apps:
  casks:
    - name: obs-ndi  # obsidian note-taking
    - name: quicksilver
    - name: slack
    - name: spotify

font_dir: "{{ ansible_user_dir }}/Library/Fonts"

root_group: wheel

xcode_version: "15.4"
